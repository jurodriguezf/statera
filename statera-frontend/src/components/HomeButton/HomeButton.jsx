import React from "react";

const HomeButton = (props) => {
    return (
        <button className="btn-home">
            <img src={""} alt={"Statera logo"}/>
            Statera
        </button>
    );
};

export default HomeButton